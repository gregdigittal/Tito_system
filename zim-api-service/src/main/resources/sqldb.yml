spring:
  datasource:
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    url: ${legacy-db.url}
    username: ${legacy-db.username}
    password: ${legacy-db.password}
  jpa:
    database-platform: org.hibernate.dialect.SQLServerDialect
    open-in-view: false
    show-sql: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
---
spring.config.activate.on-profile: local
legacy-db:
  url: jdbc:sqlserver://localhost:1433;databaseName=Test_Concerto_Zim;encrypt=false
  username: SA
  password: 123456qQ
