apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: payments-zim-api-ingress
  namespace: prod
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: zw-api.icecash.mobi
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: payments-zim-api-service
                port:
                  number: 8592
