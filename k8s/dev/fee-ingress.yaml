apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: payments-fee-ingress
  namespace: dev
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: test-mz-fee.digittal.mobi
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: payments-fee-service
                port:
                  number: 8583
