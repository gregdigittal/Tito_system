apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: payments-posb-ingress
  namespace: dev
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: test-zw-posb.digittal.mobi
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: payments-posb-service
                port:
                  number: 8293
