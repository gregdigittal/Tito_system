apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: payments-sync-ingress
  namespace: dev
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: test-zw-sync.digittal.mobi
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: payments-sync-service
                port:
                  number: 8582
