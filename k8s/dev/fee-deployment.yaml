apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: payments-fee-service
  name: payments-fee-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payments-fee-service
  strategy: {}
  template:
    metadata:
      labels:
        app: payments-fee-service
    spec:
      containers:
        - env:
            - name: SPRING_PROFILES_ACTIVE
              value: dev-k8s
          envFrom:
            - configMapRef:
                name: payments-config-map
            - secretRef:
                name: payments-secret
          image: dockerrepo.icecash.mobi:5000/payments/fee-service:0.1.0.29
          name: payments-fee-service
          ports:
            - containerPort: 8283
          resources:
            limits:
              cpu: '1'
              memory: 1G
            requests:
              cpu: 100m
              memory: 512Mi
      restartPolicy: Always
      imagePullSecrets:
        - name: dockerreg
