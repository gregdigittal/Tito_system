apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: payments-ecocash-ingress
  namespace: dev
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: test-zw-ecocash.digittal.mobi
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: payments-ecocash-service
                port:
                  number: 8586
