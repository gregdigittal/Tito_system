apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: payments-paygo-ingress
  namespace: dev
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: test-zw-paygo.digittal.mobi
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: payments-paygo-service
                port:
                  number: 8585
