apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: payments-mpesa-ingress
  namespace: dev
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: test-mz-mpesa.digittal.mobi
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: payments-mpesa-service
                port:
                  number: 8589
