apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: payments-ledger-ingress
  namespace: dev
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: test-mz-ledger.digittal.mobi
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: payments-ledger-service
                port:
                  number: 8584
