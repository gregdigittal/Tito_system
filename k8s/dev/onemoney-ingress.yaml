apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: payments-onemoney-ingress
  namespace: dev
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: test-zw-onemoney.digittal.mobi
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: payments-onemoney-service
                port:
                  number: 8587
