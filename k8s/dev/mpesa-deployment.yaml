apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: payments-mpesa-service
  name: payments-mpesa-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payments-mpesa-service
  strategy: {}
  template:
    metadata:
      labels:
        app: payments-mpesa-service
    spec:
      containers:
        - env:
            - name: SPRING_PROFILES_ACTIVE
              value: dev-k8s
          envFrom:
            - configMapRef:
                name: payments-config-map
            - secretRef:
                name: payments-secret
          image: dockerrepo.icecash.mobi:5000/payments/mpesa-service:0.1.0.11
          name: payments-mpesa-service
          ports:
            - containerPort: 8289
          resources:
            limits:
              cpu: '1'
              memory: 1G
            requests:
              cpu: 100m
              memory: 512Mi
      restartPolicy: Always
      imagePullSecrets:
        - name: dockerreg
